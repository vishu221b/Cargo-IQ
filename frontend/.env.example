# Base URL of the cargo-iq API. Leave empty in dev to use the Vite proxy
# (/api -> http://localhost:8080). Set it for a deployed build, e.g.
# VITE_API_BASE_URL=https://api.cargo-iq.example.com
VITE_API_BASE_URL=
